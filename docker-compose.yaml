services:
  app-simpleapps:
    container_name: ct-simpleapps
    build:
      context: ./apps
      dockerfile: dockerfile
    ports:
      - "5000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - ./apps/.env
    restart: always